<?xml version="1.0" encoding="ISO-8859-1" ?>

<!DOCTYPE struts-config PUBLIC
          "-//Apache Software Foundation//DTD Struts Configuration 1.3//EN"
          "http://struts.apache.org/dtds/struts-config_1_3.dtd">


<struts-config>

	<!-- ================================================ Bean Definitions -->
	<form-beans>
		<form-bean name="LoginForm" type="com.rs.fer.form.LoginForm" />

		<form-bean name="RegistrationForm"
			type="com.rs.fer.form.RegistrationForm" />

		<form-bean name="AddExpenseForm"
			type="com.rs.fer.form.AddExpenseForm" />

		<form-bean name="EditExpenseForm"
			type="com.rs.fer.form.EditExpenseForm" />
			
		<form-bean name="DeleteExpenseForm"
			type="com.rs.fer.form.DeleteExpenseForm" />
			
		<form-bean name="ExpenseReportForm"
			type="com.rs.fer.form.ExpenseReportForm" />
		
		<form-bean name="ResetPasswordForm"
			type="com.rs.fer.form.ResetPasswordForm" />

		<form-bean name="UpdateProfileForm"
			type="com.rs.fer.form.UpdateProfileForm" />



	</form-beans>
	<!-- ========================================= Global Exception Definitions -->

	<global-exceptions>
		<!-- sample exception handler <exception key="expired.password" type="app.ExpiredPasswordException" 
			path="/changePassword.jsp"/> end sample -->
	</global-exceptions>
	<!-- =========================================== Global Forward Definitions -->
	<global-forwards>
		<!-- Default forward to "Welcome" action -->
		<!-- Demonstrates using index.jsp to forward -->
		<forward name="welcome" path="/Welcome.do" />
		<forward name="dashboard" path="dashboard" />
	</global-forwards>
	<!-- =========================================== Action Mapping Definitions -->

	<action-mappings>

		<action path="/login" name="LoginForm"
			type="com.rs.fer.action.LoginAction" input="/Login.jsp">
			<forward name="login" path="/Login.jsp" />
		</action>

		<action path="/registration" name="RegistrationForm"
			type="com.rs.fer.action.RegistrationAction" input="/Registration.jsp">
			<forward name="login" path="/Login.jsp" />
			<forward name="registration" path="/Registration.jsp" />
		</action>

		<action path="/addExpense" name="AddExpenseForm"
			type="com.rs.fer.action.AddExpenseAction" input="addExpense"
			parameter="method">
			<forward name="addExpense" path="addExpense" />
		</action>

		<action path="/editExpense" name="EditExpenseForm"
			type="com.rs.fer.action.EditExpenseAction" input="editExpense"
			parameter="method">
			<forward name="editExpenseOptions" path="editExpenseOptions" />
			<forward name="editExpense" path="editExpense" />
		</action>
		
		<action path="/deleteExpense" name="DeleteExpenseForm"
			type="com.rs.fer.action.DeleteExpenseAction" input="deleteExpense"
			parameter="method">
			<forward name="deleteExpenseOptions" path="deleteExpenseOptions" />
		</action>

		<action path="/resetPassword" name="ResetPasswordForm"
			type="com.rs.fer.action.ResetPasswordAction" input="resetPassword"
			parameter="method">
			<forward name="resetPassword" path="resetPassword" />
		</action>
		
		<action path="/expenseReport" name="ExpenseReportForm"
			type="com.rs.fer.action.ExpenseReportAction" input="expenseReport"
			parameter="method">
			<forward name="expenseReportSelection" path="expenseReportSelection" />
			<forward name="expenseReport" path="expenseReport" />
			
		</action>
		

		<action path="/updateProfile" name="UpdateProfileForm"
			type="com.rs.fer.action.UpdateProfileAction" parameter="method">
			<forward name="nameInfo" path="nameInfo" />
			<forward name="contactInfo" path="contactInfo" />
			<forward name="addressInfo" path="addressInfo" />
			<forward name="review" path="review" />
		</action>


	</action-mappings>

	<!-- ======================================== Message Resources Definitions -->

	<message-resources parameter="MessageResources" />



	<!-- =============================================== Plug Ins Configuration -->

	<!-- ======================================================= Tiles plugin -->
	<!-- This plugin initialize Tiles definition factory. This later can takes 
		some parameters explained here after. The plugin first read parameters from 
		web.xml, thenoverload them with parameters defined here. All parameters are 
		optional. The plugin should be declared in each struts-config file. - definitions-config: 
		(optional) Specify configuration file names. There can be several comma separated 
		file names (default: ?? ) - moduleAware: (optional - struts1.1) Specify if 
		the Tiles definition factory is module aware. If true (default), there will 
		be one factory for each Struts module. If false, there will be one common 
		factory for all module. In this later case, it is still needed to declare 
		one plugin per module. The factory will be initialized with parameters found 
		in the first initialized plugin (generally the one associated with the default 
		module). true : One factory per module. (default) false : one single shared 
		factory for all modules - definitions-parser-validate: (optional) Specify 
		if xml parser should validate the Tiles configuration file. true : validate. 
		DTD should be specified in file header (default) false : no validation Paths 
		found in Tiles definitions are relative to the main context. To use this 
		plugin, download and add the Tiles jar to your WEB-INF/lib directory then 
		uncomment the plugin definition below. <plug-in className="org.apache.struts.tiles.TilesPlugin" 
		> <set-property property="definitions-config" value="/WEB-INF/tiles-defs.xml" 
		/> <set-property property="moduleAware" value="true" /> </plug-in> -->


	<!-- =================================================== Validator plugin -->

	<plug-in
		className="org.apache.struts.validator.ValidatorPlugIn">
		<set-property property="pathnames"
			value="/WEB-INF/validator-rules.xml,
               /WEB-INF/validation.xml" />
	</plug-in>
	<plug-in className="org.apache.struts.tiles.TilesPlugin">
		<set-property property="definitions-config"
			value="/WEB-INF/tiles-defs.xml" />
		<set-property property="moduleAware" value="true" />
	</plug-in>

</struts-config>
